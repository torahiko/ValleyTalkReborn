# 上下文注入链路审计报告

- 工单类型：只读探测
- 审计基准 commit：`74669538fcfaad01d9fdb5055f5c8e74c8232ec7`
- 审计范围：`Prompts.cs`、`LlmDialogueService.cs`、`AsyncBuilder.cs`、`PromptDeduplicator`、`DialogueHistoryManager.cs`、`SessionCache.cs`、`PlayerProfileManager.cs`
- 说明：本报告只记录代码事实与代码引用，不包含设计建议。

## 第 1 部分：完整块清单

### 1.1 请求级顶层块

实际请求装配顺序由 `LlmDialogueService.cs:183-195` 与 `LlmDialogueService.cs:325-331` 固定为：

1. `SystemPrompt`
2. `GameConstantContext`
3. `NpcConstantContext`
4. `CorePrompt + Instructions + Command`
5. `ResponseStart`

`LlmDialogueService.cs:183-195`：

```csharp
streamResult = await Llm.Instance.RunStreamingInference(
    prompts.SystemPrompt,
    prompts.GameConstantContext,
    prompts.NpcConstantContext,
    $"{prompts.CorePrompt}{prompts.Instructions}{prompts.Command}",
    ...
    prompts.ResponseStart);
```

| 块名 | 来源文件 | 数据来源 | 注入方法 | 语义归属 | 优先级标注 | 是否可抑制 |
|---|---|---|---|---|---|---|
| `SystemPrompt` 基础文本 | `Prompts.cs:204-210` | `Util.GetString(Character, "systemPrompt")`、`systemPromptTranslation` | `SystemPrompt` 属性 `Prompts.cs:95`；请求参数 `LlmDialogueService.cs:184,326` | E；A | 有：`LlmDialogueService.cs:65-69` 标注“静态在前” | 否 |
| NPC 记忆与规则段 | `LlmDialogueService.cs:71-75` | `MemoryManager.Instance.GetSmartMemoryContext(character.Name)` | 非空时拼接到 `prompts.SystemPrompt` | A；E | 有：标注 S1、静态在前 | 仅在返回空串时不注入 |
| `gossip` 传闻段 | `LlmDialogueService.cs:82-87`；`PerceptionInjector.cs:131-187` | `PerceptionInjector.BuildGossipBlock` | 非空时拼接到 `prompts.SystemPrompt` | C；D | 有：标注 S4、中频内容 | 无候选、已当日去重或空串时不注入 |
| `GameConstantContext` | `Prompts.cs:212-224`；属性 `Prompts.cs:98` | `gameContext`、`BuildStardewSummary()`、可选 `FarmStateScanner.BuildFarmSummary()` | 请求参数 `LlmDialogueService.cs:185,327` | D | 有：`LlmDialogueService.cs:65-67` 的静态/动态顺序说明覆盖顶层顺序 | `IncludeFarmDetails=false` 时抑制农场摘要；其余无分支抑制 |
| `BuildStardewSummary` 世界摘要 | `Prompts.cs:86-92`；`GameSummaryBuilder.cs:160-226` | `GameSummary` 资产、当前季节/区域/节日上下文 | `GetGameConstantContext` 内 `Prompts.cs:216` | D | 无 | 由 `BuildContext` 与 `SectionOrder` 控制，不是请求级 suppress |
| `FarmSummary` | `Prompts.cs:217-222`；`FarmStateScanner.cs:19+` | 农场状态扫描缓存 | `GetGameConstantContext` 内 `Prompts.cs:219-221` | D | 无 | `CurrentFlags.IncludeFarmDetails=false` 或摘要空时抑制 |
| `NpcConstantContext` | `Prompts.cs:226-302`；属性 `Prompts.cs:101` | NPC intro、Biography、可见 Traits、Current State、每日 Relationships、`BiographyEnd` | 请求参数 `LlmDialogueService.cs:186,328` | A；C | 无 | Biography 长度 `<=10` 时只保留 intro；Traits/Relationships 各自为空时不输出对应小节 |
| `CorePrompt` | `Prompts.cs:432-765`；属性 `Prompts.cs:104` | `GetCorePrompt` 的四条分支及全部内嵌块 | 与 `Instructions`、`Command` 拼接 `LlmDialogueService.cs:187,329` | A-E | 无 | 由四条分支早返回决定内容 |
| `Instructions` | `Prompts.cs:1698-1762`；属性 `Prompts.cs:124` | 固定指令、响应开关、额外头像说明、`Llm.Instance.ExtraInstructions` | 追加在 `CorePrompt` 后：`LlmDialogueService.cs:187,329` | E | 无 | `EnableSuggestedResponses` 只切换两类格式分支，不整体抑制 |
| `Command` | `Prompts.cs:1635-1696`；属性 `Prompts.cs:118` | 行程替换、动作标签规则、翻译提示 | 追加在 `Instructions` 后：`LlmDialogueService.cs:187,329` | E | 无 | `ReplaceSchedule` 仅在 `Context.ScheduleLine` 存在时出现；动作标签段受配置门控 |
| `ResponseStart` | `Prompts.cs:1764-1768`；属性 `Prompts.cs:121` | `I18n.ResponseStart()`，空值回退固定文本 | 单独请求参数 `LlmDialogueService.cs:195,330` | E | 无 | 否 |

### 1.2 `Prompts.cs` 全部 `Get*` 方法

下表覆盖方法声明扫描得到的全部 `Get*` 方法。`GetFriendshipText` 与 `GetSensoryWeatherDescription` 不直接调用 `AppendLine`，但其返回值由调用方写入 prompt。

| `Get*` 方法 | 定义行 | 生成/写入的 prompt 内容 | 注入点 | 语义归属 | 优先级标注 | 抑制条件 |
|---|---:|---|---|---|---|---|
| `GetSystemPrompt` | `Prompts.cs:204` | 系统设定与翻译说明 | `Prompts.cs:95`；`LlmDialogueService.cs:184,326` | E；A | 有，静态前缀 | 无 |
| `GetGameConstantContext` | `Prompts.cs:212` | 世界事实、摘要、可选农场摘要 | `Prompts.cs:98`；`LlmDialogueService.cs:185,327` | D | 有，顶层静态/动态顺序 | `IncludeFarmDetails` 控制农场摘要 |
| `GetNpcConstantContext` | `Prompts.cs:226` | NPC 身份、传记、特质、状态、关系 | `Prompts.cs:101`；`LlmDialogueService.cs:186,328` | A；C | 无 | Biography、Traits、Relationships 各自内容门控 |
| `GetMicroEnvironment` | `Prompts.cs:351` | 场景标题、位置、节日、时间季节、天气、物件、共处状态、附近村民、POI | `Prompts.cs:462,556,596,639` | D | 无 | `IncludeEnvironment=false` 只抑制附近村民；其他子项按数据为空抑制 |
| `GetSensoryWeatherDescription` | `Prompts.cs:408` | 将天气键转为感官描述 | 返回值写入 `Prompts.cs:375` | D | 无 | 无匹配天气且原值为空时返回空串 |
| `GetCorePrompt` | `Prompts.cs:432` | 四条流水线的 Core 主体 | `Prompts.cs:104`；`LlmDialogueService.cs:187,329` | A-E | 无 | 由路由分支返回 |
| `GetPreoccupation` | `Prompts.cs:969` | 私人挂念与不可知说明 | `DefaultOrOverride("Preoccupation", ...)`，`Prompts.cs:710` | A | 无 | 情绪系统且 `CurrentTodayScene != null`、玩家已说话、50% 随机、池为空时抑制 |
| `GetCurrentConversation` | `Prompts.cs:1025` | 当前对话标题、intro、可见窗口内逐行历史 | `Prompts.cs:570,597,755` | E | 无 | `ChatHistory` 空且未刚说话时不输出；DATE 直调，GREETING/FULL 可被 override |
| `GetSpecialRelationshipStatus` | `Prompts.cs:1294` | 约会、订婚、离婚、求婚拒绝状态 | `Prompts.cs:674-675`，FULL 非婚姻分支 | B；C | 无 | friendship 为空抑制；订婚行受 `PendingMilestoneBlock` 空值门控 |
| `GetSpouse` | `Prompts.cs:1318` | 其他配偶、室友、订婚者、多配偶状态 | `Prompts.cs:665,673` | B；C | 无 | 无相应 friendship 记录时无输出 |
| `GetNonSpouseFriendshipLevel` | `Prompts.cs:1408` | 当前好感度档位文本 | `Prompts.cs:672`，FULL 非婚姻分支 | B | 无 | `GetFriendshipText` 返回空时抑制 |
| `GetFriendshipText` | `Prompts.cs:1423` | 按 hearts/child/dating 选档文本 | 被 `Prompts.cs:1418` 调用并写入 `1420` | B | 无 | 由参数选择，无独立 suppress |
| `GetSpouseAction` | `Prompts.cs:1447` | 配偶离开/返回/房间等行动 | `Prompts.cs:681` | A；D | 无 | `Context.Accept` 或 `SpouseAct` 为空时抑制 |
| `GetGift` | `Prompts.cs:1463` | 玩家送礼事实 | `Prompts.cs:680` | B；E | 无 | `giveGift` 为空时抑制；`giveGift` 在 `Prompts.cs:155,186-201` 生成 |
| `GetSpecialDatesAndBirthday` | `Prompts.cs:1477` | 特殊日期与生日 | `Prompts.cs:679` | D；A | 无 | 日期不匹配时 switch 输出空串；生日另受生日条件控制 |
| `GetRecentEvents` | `Prompts.cs:1511` | 近 7 日活动事件 | `Prompts.cs:678` | C；E | 无 | `allPreviousActivities` 为空或没有匹配活动时抑制 |
| `GetMarriageFeelings` | `Prompts.cs:1539` | 婚姻/室友情感状态 | `Prompts.cs:668`，FULL 已婚分支 | B；A | 无 | 无当前 NPC friendship 记录时抑制 |
| `GetTrinkets` | `Prompts.cs:1559` | 仙子盒、青蛙、鹦鹉 | `Prompts.cs:666-667`，FULL 已婚且 `IncludeFarmDetails` | D；A | 无 | 无 trinket 或无已识别效果时抑制 |
| `GetChildren` | `Prompts.cs:1588` | 子女数量、姓名年龄、怀孕 | `Prompts.cs:663`，FULL 已婚非室友 | B；C | 无 | 无子女时仍输出 `childrenNone`；怀孕行按 `DaysUntilBirthing` |
| `GetEventHistory` | `Prompts.cs:1618` | 事件历史 | `Prompts.cs:452-453`，四条分支前 | E；C | 无 | `flags.IncludeMemories != true` 时抑制 |
| `GetGameState` | `Prompts.cs:1623` | Kent 是否归来的年份状态 | `Prompts.cs:450`，四条分支前 | C；A | 无 | 无 |
| `GetCommand` | `Prompts.cs:1635` | 指令头、行程替换、动作标签、翻译 | `LlmDialogueService.cs:187,329` | E | 无 | 行程替换受 `Context.ScheduleLine` 控制；动作标签受配置控制 |
| `GetInstructions` | `Prompts.cs:1698` | 格式、接话、选项、情绪标签、头像与额外指令 | `LlmDialogueService.cs:187,329` | E | 无 | 响应规则按 `EnableSuggestedResponses` 分流；额外指令空时抑制 |
| `GetResponseStart` | `Prompts.cs:1764` | 模型续写起点 | `LlmDialogueService.cs:195,330` | E | 无 | 本地化值非空时使用，否则固定回退 |

### 1.3 四条分支内不属于 `Get*` 的注入块

| 块名 | 定义/注入点 | 数据来源 | 语义归属 | 优先级标注 | 抑制条件 |
|---|---|---|---|---|---|
| `<emotional_conflict type="stood_up">` | `Prompts.cs:455-461` | `HasStoodUpPending` | A；E | 无 | DATE 冲突时先清除 stood-up 标志 `Prompts.cs:439-448` |
| `<date_context>` | `Prompts.cs:500-550` | `DateManager` mode、digest、gift | D；E | 无 | 非约会或日期系统关闭时抑制 |
| `<greeting_fast_pass>` | `Prompts.cs:589-595` | `IsSimpleGreeting` | E | 无 | 有 movement 或有 `PendingMilestoneBlock` 时不进入该分支 |
| `coreInstructionHeading` | `Prompts.cs:638` | 本地化 heading | E | 无 | 仅 FULL 分支 |
| `AppendCompanionWalkingContext` | `Prompts.cs:336-349`；调用 `641-644` | `CompanionFocus.RegularFollow` | D；E | 无 | 非 RegularFollow 时抑制 |
| `InjectGreetingContext` | `Prompts.cs:1077-1111`；调用 `646` | `DialogueHistoryManager.GetRecentHistory(1)` | E | 无 | 已婚/室友、无历史、今日已说话时抑制 |
| 已婚/室友/子女/配偶/挂饰/婚姻情感块 | `Prompts.cs:652-669` | friendship、children、trinket | B；C；A | 无 | 仅 FULL；按婚姻状态与 flags 分流 |
| 非婚姻好感/配偶/特殊关系块 | `Prompts.cs:670-676` | friendship | B；C | 无 | 仅 FULL 非婚姻分支 |
| `RecentEvents`、`SpecialDatesAndBirthday`、`Gift`、`SpouseAction` | `Prompts.cs:678-681` | 各 `Get*` | E；D；B | 无 | 仅 FULL；各方法内部条件 |
| `<interaction_state>` | `Prompts.cs:683-697` | `IsActiveTurn`、`Accept`、`IncludeShortTermContext` | E | 无 | 仅 FULL；两个条件分支各自门控 |
| `<date_invitation_protocol>` | `Prompts.cs:767-794`；四分支调用 `492,580,630,699` | `IsInviteRequested`、日期系统、节日 | E | 无 | 非邀请或已在约会时抑制 |
| `<jealousy_trigger>` | `Prompts.cs:701-708` | `IsJealousy`、`ActiveDateNpcName` | C；E | 无 | 仅 FULL；日期系统关闭或非 jealousy 时抑制 |
| `PlayerProfileManager.BuildProfileText` | GREETING `Prompts.cs:623-628`；FULL `714-722` | 玩家画像、safety、character anchor、玩家规则 | B | 无 | `EnablePlayerProfile=false` 或输出空时抑制 |
| `InjectSessionContinuity` | `Prompts.cs:1113-1171`；DATE/GREETING/FULL `571,598,756` | `SessionCache.RecentTurns`、`EmotionalTone` | E | 无 | 空、物理过期、跨游戏日、候选全重复时抑制 |
| `InjectPendingTopic` | `Prompts.cs:1056-1075`；四分支 `463,572,599,757` | `PendingTopicManager.ConsumePendingTopic` | A；E | 无 | 无 pending topic 时抑制 |
| `InjectMovementInstruction` | `Prompts.cs:1173-1292`；FULL `758` | `ContextFlags`、环境资产 | E；D | 无 | `MovementInstructionApplicable` 为 false 时抑制 |
| `AppendFollowInvitationProtocol` | `Prompts.cs:796-831`；STOOD/DATE `493,581`，FULL 由 movement `1181` | `ActionTag.Follow`、已有同行状态 | E | 无 | 非 Follow 或已 following 时无输出；他人同行时输出 `<follow_unavailable>` |
| `AppendDateEndingProtocol` | `Prompts.cs:833-845`；DATE `582` | `IsOnDate` | E | 无 | 非约会时抑制 |

### 1.4 `Pending*Block` 总览

| 块名 | 声明 | 写入点 | 主要数据来源 | 注入分支 | 语义归属 | 优先级标注 | 可抑制/空值条件 |
|---|---:|---:|---|---|---|---|---|
| `PendingEvolvedTraitsBlock` | `Prompts.cs:106` | `LlmDialogueService.cs:80` | `EvolvedTraitManager.GetPromptBlock` | 四条分支均读取 | A | 无 | 无 traits 且无 mindset 时 `null`；`EvolvedTraitManager.cs:302-331,430-465` |
| `PendingLocalPerceptionBlock` | `Prompts.cs:107` | `LlmDialogueService.cs:89` | `PerceptionInjector.BuildLocalBlock` | 四条分支均读取 | E；D | 无 | 无过滤后感知时 `string.Empty`；`PerceptionInjector.cs:189-192` |
| `PendingEavesdropBlock` | `Prompts.cs:108` | `LlmDialogueService.cs:94` | `EavesdropInjector.BuildBlock` | STOOD、GREETING、FULL；DATE 不读 | C；E | 无 | 无 eavesdrop 条目时 `string.Empty`；`EavesdropInjector.cs:18-27` |
| `PendingSpouseWaitingBlock` | `Prompts.cs:109` | `LlmDialogueService.cs:120-128` | `SpouseWaitingEvent` | STOOD、GREETING、FULL；DATE 不读 | A；E | 无 | 无 pending、porch context 空时保持默认 `null` |
| `PendingEchoBlock` | `Prompts.cs:110` | `LlmDialogueService.cs:145-156` | `FreshBarkBridgeStore` 优先，否则 `ImmediateEchoStore` | 四条分支均读取 | E | 无 | bridge 无行、echo 过期/错位/已消费时为空；`FreshBarkBridgeStore.cs:99-110`、`ImmediateEchoStore.cs:129-141` |
| `PendingEchoIsBridge` | `Prompts.cs:113` | `LlmDialogueService.cs:149` | bridge 命中标志 | 仅影响消费逻辑 | 元数据 | 无 | bridge 未命中时保持默认 false |
| `PendingMilestoneBlock` | `Prompts.cs:114` | `LlmDialogueService.cs:159` | `RelationshipMilestoneManager.BuildMilestoneBlock` | STOOD、DATE、FULL；GREETING 分支前置条件要求为空 | C；A；B | 有：`RelationshipMilestoneManager.cs:56-57` 声明内部优先级 | 当日已消费、非世界就绪、无匹配里程碑时 `null` |
| `PendingEmotionBlock` | `Prompts.cs:115` | `LlmDialogueService.cs:100-115` | `EmotionalStateResolver.PrepareSnapshot` + `Compile` | 四条分支均读取 | A | 无 | `EnableEmotionSystem=false` 时保持默认 null；编译异常置 `string.Empty` |

## 第 2 部分：四条流水线对比

以下单元格按每条分支的实际 `AppendLine` 顺序列出完整清单。共同顶层请求参数也列入，以保持请求级顺序完整。`—` 表示该分支没有此块；“条件”表示块本身还有内部条件。

| 注入顺序 | `STOOD_UP` | `DATE_CONTEXT` | `SIMPLE_GREETING` | `FULL_CONTEXT` |
|---:|---|---|---|---|
| 1 | 1. `SystemPrompt`（基础+记忆+gossip） | 1. `SystemPrompt`（基础+记忆+gossip） | 1. `SystemPrompt`（基础+记忆+gossip） | 1. `SystemPrompt`（基础+记忆+gossip） |
| 2 | 2. `GameConstantContext` | 2. `GameConstantContext` | 2. `GameConstantContext` | 2. `GameConstantContext` |
| 3 | 3. `NpcConstantContext` | 3. `NpcConstantContext` | 3. `NpcConstantContext` | 3. `NpcConstantContext` |
| 4 | 4. `GetGameState`（`Prompts.cs:450`） | 4. `GetGameState` | 4. `GetGameState` | 4. `GetGameState` |
| 5 | 5. `GetEventHistory`（若 `IncludeMemories`） | 5. `GetEventHistory`（若 `IncludeMemories`） | 5. `GetEventHistory`（若 `IncludeMemories`） | 5. `GetEventHistory`（若 `IncludeMemories`） |
| 6 | 6. `<emotional_conflict type="stood_up">` | 6. `<date_context mode="walking">` 或 `romantic_active` | 6. `<greeting_fast_pass>` | 6. `coreInstructionHeading` |
| 7 | 7. `GetMicroEnvironment` | 7. `GetMicroEnvironment` | 7. `GetMicroEnvironment` | 7. `GetMicroEnvironment` |
| 8 | 8. `InjectPendingTopic` | 8. `PendingEchoBlock`（条件） | 8. `GetCurrentConversation`（可 override） | 8. `AppendCompanionWalkingContext`（若 RegularFollow） |
| 9 | 9. `PendingEavesdropBlock`（条件） | 9. `PendingMilestoneBlock`（条件） | 9. `InjectSessionContinuity`（条件） | 9. `InjectGreetingContext`（条件） |
| 10 | 10. `PendingSpouseWaitingBlock`（条件） | 10. `GetCurrentConversation` | 10. `InjectPendingTopic` | 10. 婚姻/非婚姻关系块（按 friendship 分流） |
| 11 | 11. `PendingEchoBlock`（条件） | 11. `InjectSessionContinuity`（条件） | 11. `PendingEavesdropBlock`（条件） | 11. `RecentEvents`（条件） |
| 12 | 12. `PendingMilestoneBlock`（条件） | 12. `InjectPendingTopic` | 12. `PendingSpouseWaitingBlock`（条件） | 12. `SpecialDatesAndBirthday`（条件） |
| 13 | 13. `PendingEvolvedTraitsBlock`（条件） | 13. `PendingEvolvedTraitsBlock`（条件） | 13. `PendingEchoBlock`（条件） | 13. `Gift`（条件） |
| 14 | 14. `PendingLocalPerceptionBlock`（条件） | 14. `PendingLocalPerceptionBlock`（条件） | 14. `PendingEvolvedTraitsBlock`（条件） | 14. `SpouseAction`（条件） |
| 15 | 15. `PendingEmotionBlock`（条件） | 15. `PendingEmotionBlock`（条件） | 15. `PendingLocalPerceptionBlock`（条件） | 15. `<interaction_state>`（条件） |
| 16 | 16. `<date_invitation_protocol>`（条件） | 16. `<date_invitation_protocol>`（条件） | 16. `PendingEmotionBlock`（条件） | 16. `<date_invitation_protocol>`（条件） |
| 17 | 17. `AppendFollowInvitationProtocol`（内部条件） | 17. `AppendFollowInvitationProtocol`（内部条件） | 17. `PlayerProfile`（`playerInput=""`） | 17. `<jealousy_trigger>`（条件） |
| 18 | 18. `Instructions` | 18. `AppendDateEndingProtocol` | 18. `<date_invitation_protocol>`（条件） | 18. `GetPreoccupation`（可 override、条件） |
| 19 | 19. `Command` | 19. `Instructions` | 19. `Instructions` | 19. `PlayerProfile`（`playerInput=latestInput`） |
| 20 | 20. `ResponseStart` | 20. `Command` | 20. `Command` | 20. `PendingEavesdropBlock`（条件） |
| 21 | 21. — | 21. `ResponseStart` | 21. `ResponseStart` | 21. `PendingSpouseWaitingBlock`（条件） |
| 22 | 22. — | 22. — | 22. — | 22. `PendingEchoBlock`（条件） |
| 23 | 23. — | 23. — | 23. — | 23. `PendingMilestoneBlock`（条件） |
| 24 | 24. — | 24. — | 24. — | 24. `PendingEvolvedTraitsBlock`（条件） |
| 25 | 25. — | 25. — | 25. — | 25. `PendingLocalPerceptionBlock`（条件） |
| 26 | 26. — | 26. — | 26. — | 26. `PendingEmotionBlock`（条件） |
| 27 | 27. — | 27. — | 27. — | 27. `GetCurrentConversation`（可 override） |
| 28 | 28. — | 28. — | 28. — | 28. `InjectSessionContinuity`（条件） |
| 29 | 29. — | 29. — | 29. — | 29. `InjectPendingTopic` |
| 30 | 30. — | 30. — | 30. — | 30. `InjectMovementInstruction`（内部条件） |
| 31 | 31. — | 31. — | 31. — | 31. `Instructions` |
| 32 | 32. — | 32. — | 32. — | 32. `Command` |
| 33 | 33. — | 33. — | 33. — | 33. `ResponseStart` |

分支判定点：

- `STOOD_UP`：`Prompts.cs:455`
- `DATE_CONTEXT`：`Prompts.cs:500`
- `SIMPLE_GREETING`：`Prompts.cs:589`
- `FULL_CONTEXT`：默认落点 `Prompts.cs:638`

### 2.1 明确跳过/不进入的块

| 块 | STOOD_UP | DATE_CONTEXT | SIMPLE_GREETING | FULL_CONTEXT | 代码事实 |
|---|---|---|---|---|---|
| `PendingEavesdropBlock` | 读取 | 不读取 | 读取 | 读取 | DATE 仅注入 Echo/Milestone `Prompts.cs:558-568` |
| `PendingSpouseWaitingBlock` | 读取 | 不读取 | 读取 | 读取 | 同上 |
| `PendingMilestoneBlock` | 读取 | 读取 | 不进入 GREETING 分支 | 读取 | GREETING 判定要求 `string.IsNullOrEmpty(PendingMilestoneBlock)` `Prompts.cs:589` |
| `GetCurrentConversation` | 不读取 | 读取 | 读取 | 读取 | STOOD 早返回于 `Prompts.cs:497` |
| `InjectSessionContinuity` | 不读取 | 读取 | 读取 | 读取 | STOOD 分支无该调用 |
| `PlayerProfile` | 不读取 | 不读取 | 读取 | 读取 | 两个调用点 `Prompts.cs:623,714` |
| `GetPreoccupation` | 不读取 | 不读取 | 不读取 | 读取 | 仅 FULL `Prompts.cs:710` |
| `InjectMovementInstruction` | 不读取 | 不读取 | 不读取 | 读取 | 仅 FULL `Prompts.cs:758` |
| `<jealousy_trigger>` | 不读取 | 不读取 | 不读取 | 读取 | 仅 FULL `Prompts.cs:701` |
| `<interaction_state>`、关系/日期/礼物/配偶行动块 | 不读取 | 不读取 | 不读取 | 读取 | 位于 `Prompts.cs:648-697`，在各早返回之后 |

## 第 3 部分：`Pending*Block` 写入点

### 3.1 写入点、条件与消费点

| 属性名 | 声明位置 | 写入点 | 写入条件 | null/empty 条件 | 四分支消费点 | `ConfirmDynamicBlocksConsumed` |
|---|---:|---:|---|---|---|---|
| `PendingEvolvedTraitsBlock` | `Prompts.cs:106` | `LlmDialogueService.cs:80` | 每次 prompt 打包时无条件调用 `GetPromptBlock(character.Name, context)` | `GetPromptBlock` 在无 traits 时返回 `null`；mindset 分支返回基线块 | STOOD `485`、DATE `573`、GREETING `616`、FULL `748` | 否 |
| `PendingLocalPerceptionBlock` | `Prompts.cs:107` | `LlmDialogueService.cs:89` | 每次无条件调用 `BuildLocalBlock` | 无过滤后感知时 `string.Empty` | STOOD `487`、DATE `575`、GREETING `618`、FULL `750` | 否 |
| `PendingEavesdropBlock` | `Prompts.cs:108` | `LlmDialogueService.cs:94` | 每次无条件调用 `BuildBlock` | 无 eavesdrop 历史时 `string.Empty` | STOOD `465`、GREETING `601`、FULL `724`；DATE 不读 | 是：`ConsumeEavesdropEntries` |
| `PendingSpouseWaitingBlock` | `Prompts.cs:109` | `LlmDialogueService.cs:120-128` | `HasPendingSpouseDialogue` 为 true 且 `GetPorchContext()` 非空 | 其他情况保持默认 `null` | STOOD `470`、GREETING `606`、FULL `730`；DATE 不读 | 是：`ConfirmSpouseDialogueConsumed` |
| `PendingEchoBlock` | `Prompts.cs:110` | `LlmDialogueService.cs:145-156` | 先尝试 bridge；bridge 非空则覆盖，否则调用 `ImmediateEchoStore.BuildEchoBlock` | bridge null 且 immediate 无有效条目时为 null | STOOD `475`、DATE `559`、GREETING `611`、FULL `736` | 是，但仅 `PendingEchoIsBridge == false` |
| `PendingEchoIsBridge` | `Prompts.cs:113` | `LlmDialogueService.cs:149` | 仅 bridge 非空时设为 true | 默认 false | 不直接注入 | 控制 Echo 是否消费 |
| `PendingMilestoneBlock` | `Prompts.cs:114` | `LlmDialogueService.cs:159` | 每次无条件调用 `BuildMilestoneBlock` | 世界未就绪、当日已消费、无匹配事件时 null | STOOD `480`、DATE `564`、FULL `742`；GREETING 通过路由条件排除 | 是：`ConfirmConsumed` |
| `PendingEmotionBlock` | `Prompts.cs:115` | `LlmDialogueService.cs:100-115` | `EnableEmotionSystem=true` 时快照并编译 | 配置关闭保持 null；编译异常置 `string.Empty` | STOOD `489`、DATE `577`、GREETING `620`、FULL `752` | 否 |

### 3.2 `ConfirmDynamicBlocksConsumed` 完整逻辑

定义：`LlmDialogueService.cs:486-500`。

```csharp
private static void ConfirmDynamicBlocksConsumed(DialogueContext context, Character character, Prompts prompts)
{
    if (prompts == null || character == null) return;

    if (!string.IsNullOrEmpty(prompts.PendingEavesdropBlock))
        DialogueHistoryManager.Instance?.ConsumeEavesdropEntries(character.Name);
    if (!string.IsNullOrEmpty(prompts.PendingSpouseWaitingBlock))
        SpouseWaitingEvent.ConfirmSpouseDialogueConsumed(character.Name);
    if (!string.IsNullOrEmpty(prompts.PendingEchoBlock) && !prompts.PendingEchoIsBridge)
        ImmediateEchoStore.ConsumeEcho(character.Name);

    // 💍 确认本轮已表达过关系里程碑/吃醋，今日后续交谈恢复常态陪伴
    if (!string.IsNullOrEmpty(prompts.PendingMilestoneBlock))
        RelationshipMilestoneManager.Instance.ConfirmConsumed(character.Name);
}
```

调用点：

- 流式取消路径：`LlmDialogueService.cs:214`
- 流式成功路径：`LlmDialogueService.cs:285`
- 非流式取消路径：`LlmDialogueService.cs:381`
- 非流式成功路径：`LlmDialogueService.cs:417`

注释声明该函数的目的：`LlmDialogueService.cs:480-485`，包括避免 STOOD_UP / DATE_CONTEXT / SIMPLE_GREETING 早返回分支丢弃尚未展示内容。

### 3.3 `DialogueHistoryManager` 历史写入时机

- 玩家台词入口：`DialogueHistoryManager.cs:100-118`；调用点包括 `TextInputHandler.cs:127`、`Dialogue_ChooseResponse_Patch.cs:50,147`、`Event_AnswerDialogue_Patch.cs:88,127`。
- `GetTodayTurnCount` 注释明确玩家台词在生成前落库但不计数：`DialogueHistoryManager.cs:287-291`。
- `GetTodayTurnCount` 只统计当前游戏年/季/日、`SpeakerType.NPC`、非 eavesdrop、非 gift 条目：`DialogueHistoryManager.cs:292-309`。
- AsyncBuilder 中 NPC 回复在 `Game1.DrawDialogue` 后写入：`AsyncBuilder.cs:345-362`。
- LLM 服务成功后将本轮 `ChatHistory` 与清洗后的 NPC 回复合并到 `SessionCache`：流式 `LlmDialogueService.cs:273-285`，非流式 `406-417`。

## 第 4 部分：潜在冲突证据

### D1 `GetMicroEnvironment` 与 `GameConstantContext` 的场景描述

证据 A：`GameConstantContext` 的组成。

`Prompts.cs:212-223`：

```csharp
private string GetGameConstantContext()
{
    var gameConstantPrompt = new StringBuilder();
    gameConstantPrompt.AppendLine(Util.GetString(Character, "gameContext"));
    gameConstantPrompt.AppendLine(BuildStardewSummary());
    if (CurrentFlags.IncludeFarmDetails)
    {
        string farmSummary = FarmStateScanner.BuildFarmSummary(IsChineseLanguage);
        if (!string.IsNullOrEmpty(farmSummary))
            gameConstantPrompt.AppendLine(farmSummary);
    }
    return gameConstantPrompt.ToString();
}
```

`GameSummaryBuilder.cs:275-281` 对当前位置输出：

```csharp
string friendly = EnvironmentScanner.GetLocationFriendlyName(curLoc);
string desc = isZh
    ? $"当前角色所处的具体场景（{friendly}）。"
    : $"Current surroundings ({friendly}).";
builder.AppendLine($"- **{friendly}** - {desc}");
```

证据 B：`GetMicroEnvironment` 的组成。

`Prompts.cs:353-405` 写入：

- `sceneHeading`
- `sceneLocation`
- 节日
- 时间、季节、日
- 天气
- 近景物件
- 共处空间状态
- 附近村民
- POI

本地化文本样例：`ContentPack/i18n/zh.json:127-133`。

```json
"sceneHeading": "### 场景感知",
"sceneLocation": "- 位置: {{Location}}",
"sceneNearbyVillagers": "- 现场其他村民: {{Villagers}}",
"sceneObjects": "- 近景物件: {{Objects}}",
"sceneSpatialCoPresent": "- 空间状态: 你和农夫就在同一处，面对面。",
"sceneTimeSeason": "- 时间: {{Time}} | 季节: {{Season}}（第 {{Day}} 天）",
"sceneWeather": "- 天气氛围: {{Weather}}",
```

事实：两处都可能包含由 `EnvironmentScanner.GetLocationFriendlyName` 得到的当前位置。`GameConstantContext` 位于请求第 2 参数，`GetMicroEnvironment` 位于 `CorePrompt`，两处在同一次请求中都可出现。

### D2 `GetCurrentConversation` 与 `InjectSessionContinuity`

`GetCurrentConversation` 的注入逻辑：`Prompts.cs:1025-1054`。

```csharp
if (Context.ChatHistory.Any())
{
    prompt.AppendLine(ComposeCurrentConversationHeading());
    ...
    int configured = Math.Clamp(ModEntry.Config?.PromptHistoryWindow ?? 6, 1, 20);
    int window = Math.Clamp(shortCtxAllowed ? configured : 1, 1, ...);
    var visible = Context.ChatHistory.Count > window
        ? Context.ChatHistory.GetRange(...)
        : Context.ChatHistory;
    foreach (var elem in visible)
        prompt.AppendLine(... elem.Text ...);
}
```

`InjectSessionContinuity` 的去重逻辑：`Prompts.cs:1113-1149`。

```csharp
var session = SessionCache.Instance.GetOrCreate(Character.Name);
if (session.RecentTurns.Count == 0) return;
if (... LastUpdatedYear/Season/Day != 当前游戏日期 ...) return;

string Normalize(string s) { ... 删除玩家名与空白、`$ # @ { } [ ] ()` ... }

var currentNorms = Context.ChatHistory
    .Select(x => Normalize(x.Text))
    .Where(x => !string.IsNullOrEmpty(x))
    .ToHashSet();

var candidateTurns = session.RecentTurns
    .Where(t => !string.IsNullOrWhiteSpace(t.Text) && !currentNorms.Contains(Normalize(t.Text)))
    .ToList();

if (Context.ChatHistory.Any() && candidateTurns.Count > 0)
{
    int skipRecentCount = Math.Min(2, candidateTurns.Count);
    candidateTurns = candidateTurns.Take(candidateTurns.Count - skipRecentCount).ToList();
}
...
var previousTurns = candidateTurns.TakeLast(3).ToList();
```

`SessionCache.MergeHistory` 的来源：`SessionCache.cs:49-71`，把 `context.ChatHistory` 全部元素和清洗后的 `npcReply` 合并进 `RecentTurns`，并按“文本+方向”去重。

事实：

- `GetCurrentConversation` 不读取 `SessionCache`。
- `InjectSessionContinuity` 会排除与 `Context.ChatHistory` 归一化文本相同的 session 条目。
- `InjectSessionContinuity` 在 `Context.ChatHistory.Any()` 时还会去掉 session 候选中最近 2 条，再取最后 3 条。
- 两者的注入点在 DATE/GREETING/FULL 中相邻或同属尾部：DATE `Prompts.cs:570-572`、GREETING `597-599`、FULL `755-757`。

### D3 `NpcConstantContext` 与 `PendingEvolvedTraitsBlock`

`NpcConstantContext` 可写入：

- Biography：`Prompts.cs:232-241`
- Bio Traits：`Prompts.cs:243-263`
- Current State：`Prompts.cs:266-276`
- Relationships：`Prompts.cs:278-298`

Traits 示例条件：

```csharp
int currentHearts = Context.Hearts ?? 0;
var visible = Character.Bio.Traits.Values
    .Where(t => currentHearts >= t.RequiredHearts)
    .ToList();
...
npcConstantPrompt.AppendLine($"* **{heading}**: {desc}");
```

`PendingEvolvedTraitsBlock` 来源：`EvolvedTraitManager.cs:302-331`。

```csharp
sb.AppendLine(isZh
    ? "### 对农夫的长期印象（并非本次对话中发生的事，是你长期形成的背景认知，供当前对话参考。）"
    : "### IMPRESSIONS OF THE FARMER ...");
sb.AppendLine("<farmer_impressions>");
foreach (string trait in traits.Skip(...))
    sb.AppendLine($"- {trait}");
sb.AppendLine("</farmer_impressions>");
```

mindset 分支：`EvolvedTraitManager.cs:443-463`。

```csharp
"### 对农夫的常驻心智底色（长期稳定的态度基线，非本次对话事件）"
"<mindset_baseline>"
$"关系定位：{mindset.RelationshipStance}"
...
$"边界层级：{mindset.BoundaryLevel}"
```

事实：

- `NpcConstantContext` 的 Traits/Relationships 来自 `Character.Bio` 与当前 hearts/每日抽选。
- `PendingEvolvedTraitsBlock` 来自存档演化 traits 或 mindset。
- 注入路径中未发现二者的互相读取、同步或抑制代码。
- 两块分别位于请求的 `NpcConstantContext` 与 `CorePrompt`，可在同一请求并存。

### D4 `Instructions` 的“第一句必须接住玩家”与 STOOD_UP

`GetInstructions` 固定文本：`Prompts.cs:1720-1722`。

```csharp
instructions.AppendLine(isZh
    ? "- 【当面接话】农夫刚说的那句话就在你耳边。第一句必须直接回答或接住他刚才的话；答完之后，手头有事或想起别的事再顺着往下提。"
    : "- [IN-PERSON REPLY] The farmer's last words just reached your ears. Your very first sentence must directly address or answer what they just said. ...");
```

写入点扫描结果：

```text
Prompts.cs:124  Instructions => GetInstructions()
Prompts.cs:1698 GetInstructions()
```

未发现 `prompts.Instructions =` 或 `Instructions +=` 的其他写入点。

STOOD_UP 分支：

- 判定：`Prompts.cs:455`
- 早返回：`Prompts.cs:495-497`
- 分支内没有修改 `Instructions`
- `LogTopologyVerification` 明确 STOOD_UP 不要求历史：`Prompts.cs:874,884-886`

请求级装配仍为：

```csharp
$"{prompts.CorePrompt}{prompts.Instructions}{prompts.Command}"
```

证据位置：`LlmDialogueService.cs:187,329`。

事实：STOOD_UP 返回的 `CorePrompt` 后仍拼接同一份未改写的 `Instructions`。

### D5 `Preoccupation` 与 `PendingEmotionBlock` 守卫

`GetPreoccupation` 完整前置守卫：`Prompts.cs:969-976`。

```csharp
private void GetPreoccupation(StringBuilder prompt)
{
    // 情绪系统场景已含 [脑中主要挂念]，避免双重挂念
    if (ModEntry.Config.EnableEmotionSystem && Character?.CurrentTodayScene != null) return;

    bool playerHasSpoken = Context.ChatHistory.Any(x => x.IsPlayerLine);
    if (playerHasSpoken) return;
    if (Game1.random.NextDouble() < 0.5) return;
    ...
}
```

`PendingEmotionBlock` 写入：`LlmDialogueService.cs:100-115`。

```csharp
if (ModEntry.Config.EnableEmotionSystem)
{
    try
    {
        var snapshot = EmotionalStateResolver.PrepareSnapshot(character, character.StardewNpc);
        character.PendingEmotion = snapshot;
        prompts.PendingEmotionBlock = EmotionalStateResolver
            .Compile(character, character.StardewNpc, snapshot).PromptBlock;
    }
    catch (Exception ex)
    {
        prompts.PendingEmotionBlock = string.Empty;
        ...
    }
}
```

事实：

- `Preoccupation` 的互斥守卫需要同时满足 `EnableEmotionSystem` 与 `CurrentTodayScene != null`。
- 情绪系统开启但 `CurrentTodayScene == null` 时，该守卫不返回。
- `PendingEmotionBlock` 不依赖 `CurrentTodayScene` 才写入；`PrepareSnapshot` 仅将 `CurrentTodayScene` 作为 Bias 输入之一：`EmotionalStateResolver.cs:66-71`。
- 两者在 FULL 分支分别位于 `Prompts.cs:710` 与 `752`。

### D6 `PendingMilestoneBlock` 在 GREETING 分支

GREETING 入口条件：`Prompts.cs:589`。

```csharp
if (flags?.IsSimpleGreeting == true && flags?.IsMovementRequested != true && string.IsNullOrEmpty(PendingMilestoneBlock))
```

事实：

- `PendingMilestoneBlock` 非空时，条件整体为 false，不进入 GREETING 分支，落到 `Prompts.cs:638` 的 FULL_CONTEXT。
- GREETING 分支内部没有针对 Milestone 的 `if` 跳过语句，因为该块根本未进入分支。
- `Prompts.cs:589` 附近未发现解释该条件意图的注释。
- 相关注释只出现在 `RelationshipMilestoneManager.cs:55-57`，内容为优先级与“注入即焚”；没有 GREETING 字样。
- `LogTopologyVerification` 对 GREETING 与 FULL 使用不同 route 名：`Prompts.cs:633-634,761-762`。

### D7 `SessionCache` 15 分钟物理时间与游戏时间语义

`SessionCache`：`SessionCache.cs:18-29,34-42`。

```csharp
private const int ExpiryMinutes = 15;
...
public DateTime LastActivity { get; set; } = DateTime.Now;
public bool IsExpired => (DateTime.Now - LastActivity).TotalMinutes > ExpiryMinutes;

public SessionEntry GetOrCreate(string npcName)
{
    if (!_cache.TryGetValue(npcName, out var entry) || entry.IsExpired)
    {
        entry = new SessionEntry();
        _cache[npcName] = entry;
    }
    entry.LastActivity = DateTime.Now;
    return entry;
}
```

游戏日期另存于：`SessionCache.cs:80-86`。

```csharp
entry.LastUpdatedYear = Game1.year;
entry.LastUpdatedSeason = (Season)Game1.season;
entry.LastUpdatedDay = Game1.dayOfMonth;
```

`InjectSessionContinuity` 在 `GetOrCreate` 后再检查游戏日期：`Prompts.cs:1115-1121`。

`GetCurrentConversation` 的 `ChatHistory` 来源：`DialogueBuilder.cs:995-1017`，从 `DialogueHistoryManager.GetRecentHistory` 取最近条目，使用游戏时间生成 `FuzzyTime`：`DialogueHistoryAdapter.cs:65-95`。

事实：

- `SessionCache` 同时有 `DateTime.Now` 的 15 分钟过期与游戏年/季/日检查。
- `GetOrCreate` 在过期时创建新条目，因此物理过期先于 `InjectSessionContinuity` 的游戏日期判断发生。
- `ChatHistory` 路径由 `DialogueHistoryManager` 条数与过滤控制，代码中未出现 15 分钟 `DateTime.Now` 过期条件。
- `FuzzyTime` 按游戏日期差与游戏分钟差生成。

### D8 `PlayerProfileManager.BuildProfileText` 两次调用

调用点 A：GREETING，`Prompts.cs:623-628`。

```csharp
string simpleProfile = PlayerProfileManager.BuildProfileText(
    Character.StardewNpc,
    playerInput: "",
    flags: flags);
if (!string.IsNullOrEmpty(simpleProfile))
    prompt.AppendLine(simpleProfile);
```

调用点 B：FULL_CONTEXT，`Prompts.cs:712-722`。

```csharp
string latestInput = Context?.ChatHistory?
    .LastOrDefault(x => x.IsPlayerLine)?.Text ?? "";
string playerProfile = PlayerProfileManager.BuildProfileText(
    Character.StardewNpc,
    playerInput: latestInput,
    flags: flags);
if (!string.IsNullOrEmpty(playerProfile))
{
    prompt.AppendLine(playerProfile);
    prompt.AppendLine();
}
```

`BuildProfileText` 参数使用：`PlayerProfileManager.cs:47-57`。

```csharp
public static string BuildProfileText(NPC targetNpc, string playerInput = "", ContextFlags flags = null)
{
    ...
    flags ??= ContextRouter.Evaluate(targetNpc, playerInput, config.RomanceSafetyMode);
```

事实：

- 两次都传入相同 `Character.StardewNpc` 与同一 `flags` 对象。
- `playerInput` 不同：GREETING 为 `""`，FULL 为最新玩家行或 `""`。
- 调用点 A 返回 GREETING 分支 `Prompts.cs:635`，不会执行 FULL 的调用点 B。
- 两个调用点位于互斥的早返回分支内，同一轮代码路径中不会各执行一次。

## 第 5 部分：`PromptDeduplicator` 行为

### 5.1 `ProtectedPrefixes` 完整列表

定义：`LlmDialogueService.cs:798-809`。

```csharp
private static readonly string[] ProtectedPrefixes = new[]
{
    "- 位置:", "- Location:",
    "- 时间:", "- Time:",
    "- 季节:", "- Season:",
    "- 天气氛围:", "- Weather:",
    "- 空间状态:", "- Spatial Status:",
    "- 社交关系:", "- Social Standing:",
    "- 状态:", "- Status:",
    "- 目标:", "- Goal:",
    "- ("      // 保护示范台词起笔
};
```

### 5.2 作用范围

调用点：`LlmDialogueService.cs:161-162`。

```csharp
// ── 终局出口去重：优先保留下方 CorePrompt 的即时条目，剔除上方 SystemPrompt 的冗余条目 ──
PromptDeduplicator.DeduplicatePrompts(prompts);
```

实际实现：`LlmDialogueService.cs:825-845`。

```csharp
var seenEntries = new HashSet<string>(StringComparer.OrdinalIgnoreCase);

// 1. 先收集 SystemPrompt 中的条目（用于检测重复，但不修改 SystemPrompt）
CollectEntriesWithoutModifying(prompts.SystemPrompt, seenEntries);

// 2. 只清洗 CorePrompt（若与 SystemPrompt 撞车，CorePrompt 中的副本被移除）
string originalCorePrompt = prompts.CorePrompt;
prompts.CorePrompt = DeduplicateInternal(prompts.CorePrompt, seenEntries);

// 3. SystemPrompt 完全不动
// prompts.SystemPrompt 保持原样
```

范围事实：

| 内容 | 是否被收集 | 是否被修改 |
|---|---|---|
| `SystemPrompt` | 是，仅收集 | 否 |
| `CorePrompt` | 是 | 是 |
| `GameConstantContext` | 否 | 否 |
| `NpcConstantContext` | 否 | 否 |
| `Instructions` | 否 | 否 |
| `Command` | 否 | 否 |
| `ResponseStart` | 否 | 否 |

去重粒度：`LlmDialogueService.cs:872-955`。

- 只识别 `line.TrimStart()` 后以 `"- "` 开头的行。
- 内容取 `trimmed.Substring(2).Trim()`。
- 只有长度 `>= 4` 的内容参与。
- `ProtectedPrefixes` 以 `OrdinalIgnoreCase` 匹配。
- `HashSet` 使用 `StringComparer.OrdinalIgnoreCase`。
- `CorePrompt` 自身重复也会命中，因为 `seenEntries` 在处理过程中持续更新。

### 5.3 代码注释与实现方向的事实对照

调用点注释 `LlmDialogueService.cs:161` 写的是“保留下方 CorePrompt 的即时条目，剔除上方 SystemPrompt 的冗余条目”。

实现 `LlmDialogueService.cs:835-842` 写的是“收集 SystemPrompt”，随后“CorePrompt 中的副本被移除”。

两段代码文字方向相反；实现的实际结果是：同内容先由 SystemPrompt 占据 `seenEntries`，CorePrompt 的后续行被删除。

### 5.4 是否对 `Pending*Block` 生效

事实：

- `PromptDeduplicator.DeduplicatePrompts` 在 `CorePrompt` 完整构建之后调用：`LlmDialogueService.cs:162`。
- `Pending*Block` 已在 `GetCorePrompt` 内写入 `CorePrompt`。
- 去重器不读取 `Pending*` 属性名，也不记录行来源。
- 因此只对 Pending 块内部“以 `- ` 开头、长度至少 4、非保护前缀”的文本行产生文本级作用。
- XML 标签、标题、空行、受保护前缀、非 `- ` 行不参与去重。

### 5.5 已知漏判场景

代码注释与测试扫描结果：

- 未发现以 `PromptDeduplicator` 命名的测试文件。
- 未发现注释明确列出“已知漏判场景”的测试或 TODO。
- 实现条件本身给出的不覆盖范围为：非 `- ` 行、内容长度 `< 4`、`ProtectedPrefixes` 行、`SystemPrompt/CorePrompt` 之外的参数。

## 第 6 部分：`AsyncBuilder` 续体影响

### 6.1 续体中与 prompt/记录有关的操作

| 操作 | 位置 | 影响分类 | 代码事实 |
|---|---|---|---|
| 页面守卫裁剪 | `AsyncBuilder.cs:296-306` | 影响本轮显示与后续记录输入 | 裁剪发生在 `rawText` 捕获前 |
| `rawText` 捕获 | `AsyncBuilder.cs:308-309` | 记录输入 | `rawText = string.Join(" ", newDialogue.dialogues.Select(d => d.Text))`，注释标明在 narration 前 |
| 肖像码提取 | `AsyncBuilder.cs:311-314` | 情绪反馈，不直接写 prompt | `portraitCode` 只传给 `DialogueFeedbackService.EvaluateDialogueFeedback`：`409-412`；`AsyncBuilder` 内未发现写 `PendingEchoBlock` |
| 情绪 narration 拼接 | `AsyncBuilder.cs:316-343` | 影响显示，不进入本轮记录文本 | `newDialogue.dialogues[0].Text = narration + "\n" + ...`；记录仍使用事先捕获的 `rawText`：`347-351` |
| `Game1.DrawDialogue` | `AsyncBuilder.cs:345` | 显示 | 主线程续体内调用 |
| `SanitizeDialogueForHistory` | `AsyncBuilder.cs:350-351,453-461` | 只影响记录；通过记录间接影响未来 prompt | 输出用于 `RecentConversationTracker`、`DialogueHistoryManager`、eavesdrop 记录 |
| `RecentConversationTracker.RecordResponse` | `AsyncBuilder.cs:356-357` | 记录 | 当前未发现直接写 `Prompts` 字段 |
| NPC 对话历史写入 | `AsyncBuilder.cs:359-362` | 未来 prompt 的 `ChatHistory` 来源 | `RecordGiftReaction` 或 `RecordNpcDialogue` |
| 周围 NPC eavesdrop 广播 | `AsyncBuilder.cs:364-405` | 未来 prompt 的 `PendingEavesdropBlock` 来源 | 距离 `<=512f` 时 `RecordSystemEvent(..., "eavesdrop")` |
| 情绪反馈与提交 | `AsyncBuilder.cs:408-413` | 未来情绪快照输入 | `EvaluateDialogueFeedback`、`NotifyDialogueCommitted` |
| `SanitizeDialogueForHistory` 实现 | `AsyncBuilder.cs:453-461` | 记录清洗 | 删除 `Respond/回应` 残留、控制标记、单字符 `$` 标记后调用 `DialogueHistoryManager.SanitizeForStorage` |

### 6.2 “影响 prompt”与“只影响记录”的边界

当前轮 prompt：

- `AsyncBuilder` 续体发生在 `Generate*` 已返回之后。
- 续体中的 narration、肖像提取、`SanitizeDialogueForHistory` 都不写 `Prompts.Pending*Block`、`SystemPrompt`、`CorePrompt`、`Instructions`。
- 因此这些操作不修改本轮已经发出的 prompt。

未来轮 prompt：

- `RecordNpcDialogue` 的文本会经 `DialogueHistoryManager.GetRecentHistory` 进入 `DialogueBuilder.cs:999-1017` 的 `ChatHistory`。
- `ChatHistory` 再进入 `GetCurrentConversation` 与 `InjectSessionContinuity` 的去重输入。
- `RecordSystemEvent(..., "eavesdrop")` 会进入 `EavesdropInjector.BuildBlock`。
- `EvaluateDialogueFeedback` 可能改变后续 `EmotionalStateResolver` 快照输入；该链路只记录到续体调用点，未继续展开情绪内部算法。

### 6.3 `FormatLine`

在 `AsyncBuilder.cs` 内搜索 `\bFormatLine\b`：无命中。

全库命中：

- `DialogueBuilder.cs:658`：`private string FormatLine(string[] theLine, ...)`
- `DialogueBuilder.cs:383,455,512,567`：生成返回 `Dialogue` 前调用
- `NightlyConsolidationHook.cs:453`：另一个同名辅助方法

`DialogueBuilder.FormatLine` 的事实：

- 清除 `[UI:*]` 标签：`DialogueBuilder.cs:665-673`
- 清洗并校验 NPC 首段：`675-677`
- 限制翻页与字符数：`679-720`
- 可拼接原版响应占位与建议选项：`722-747`
- 其输出成为 `Dialogue.dialogues` 内容，随后在 `AsyncBuilder.cs:309` 被捕获为 `rawText`

事实结论：`FormatLine` 不在 `AsyncBuilder` 内；其输出通过 `Dialogue` 进入 `AsyncBuilder` 的记录链。

## 第 7 部分：未覆盖项 / 无法核实项

### 7.1 工单停止条件检查

| 停止条件 | 结果 | 证据 |
|---|---|---|
| 某个 `Pending*Block` 无写入点 | 未发现 | 7 个块均在 `LlmDialogueService.cs:80-159` 有写入点 |
| 出现工单未提到的第五个流水线分支 | 未发现 | 四个返回点为 `Prompts.cs:497,586,635,764` |
| `Get*` 递归调用或隐式循环 | 未发现 | 方法扫描未见 `Get*` 自调用；辅助调用为 `BuildStardewSummary`、`GetSensoryWeatherDescription`、`GetFriendshipText` 等非递归路径 |
| `Prompts.cs` 与提供版本发生版本漂移 | 无法与“提供版本”比对 | 工单未附带对照版本或 hash；本报告以当前 commit 记录 |

### 7.2 未覆盖/无法核实清单

- 未运行代码、未运行测试、未启动游戏；所有证据均为静态代码读取。
- 未获得工单所称“提供版本”的 `Prompts.cs`，因此只能记录当前版本，不能做版本差异比对。
- `AsyncBuilder.FormatLine` 在当前代码不存在；只能记录实际位于 `DialogueBuilder.cs:658` 的同名方法。
- 未对 `EvolvedTraitManager`、`RelationshipMilestoneManager`、`PerceptionInjector`、`DateManager` 的全部业务算法展开；只采集其进入 prompt 的返回值条件。
- 未展开 `EvaluateDialogueFeedback` 对未来情绪快照的内部影响链。
- 未找到 `PromptDeduplicator` 的测试文件，因此“测试中的已知漏判场景”无法核实。
- `注入耗时`：本次探测未安装独立计时器，无法提供精确逐项耗时；按执行顺序的工作量估算为 A 10 分钟、B 6 分钟、C 6 分钟、D 12 分钟、E 4 分钟、F 6 分钟、整理与引用核对 6 分钟。该数字为估算，不是计时器实测。

### 7.3 版本信息

- 审计时仓库 HEAD：`74669538fcfaad01d9fdb5055f5c8e74c8232ec7`
- 审计开始时 `git status --short` 未返回变更行。
- 本报告为新增 Markdown 交付物，不包含 C# 代码修改。

